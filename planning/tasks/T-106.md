---
id: T-106
title: 'status: active-spec drift breakdown'
status: completed
priority: medium
spec_ref: specs/v0.4.0.md#status-active-spec-drift-breakdown
dependencies:
    - T-108-next-filter-idle-selection-to-active-spec-tasks
updated_at: "2026-07-17T11:01:29Z"
---

# T-106 status: active-spec drift breakdown

## Description

Improve `taskrail status` with a concise active-spec drift breakdown, per
`specs/v0.4.0.md#status-active-spec-drift-breakdown`. The output should answer
how much open work targets the active spec versus older/other specs, and the
computed `next` line should mirror the active-spec-filtered idle selection from
`specs/v0.4.0.md#active-spec-filtered-next-selection` without persisting it.

## Acceptance

- Human `status` output includes a compact active-spec drift line with counts of
  open tasks linked to the active spec and open tasks linked away from it.
- When away-from-active open work exists, human output names each affected task
  id and its `spec_ref` in an inspectable section.
- `status --json` mirrors the breakdown with structured counts and task/spec-ref
  pairs.
- Only open work is counted: `todo`, `in_progress`, and `blocked` tasks count;
  `completed` and `cancelled` tasks remain delivered history and are excluded.
- The computed `next` line uses the same active-spec-filtered idle selection as
  `next`, while the existing active-task drift warning remains unchanged.
- `status` stays strictly read-only, with no writes to `planning/STATE.md` or task
  files.
- README/help text documents the new status signal without describing it as a
  scheduler; when mentioning filtering, it must match the `next` active-spec
  selection contract.

## Verification Notes

- TODO: record verification evidence paths.

## Implementation Notes

- Depends on T-108-next-filter-idle-selection-to-active-spec-tasks because the
  status next line must mirror the revised filtered idle selector.
- Planning-only breakdown task created from the v0.4.0 spec update; no product
  code has been implemented yet.
- 2026-07-17T11:01:29Z: verification pass
- 2026-07-17T11:01:29Z: implemented active-spec drift breakdown in status.go + renderer; README/CHANGELOG/help updated
