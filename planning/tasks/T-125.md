---
id: T-125
title: Heal duplicate Implementation Notes heading in T-110 task file
status: todo
priority: medium
spec_ref: specs/v0.2.0.md#no-local-paths-in-task-notes
dependencies: []
updated_at: "2026-07-27T22:29:19Z"
---

# T-125 Heal duplicate Implementation Notes heading in T-110 task file

## Description

`planning/tasks/T-110-next-include-off-spec.md` carries two consecutive
`## Implementation Notes` headings (lines 48 and 50), so
`task check:task-bodies` (`TestCorpusTasksHaveOneImplementationNotesSection`)
fails on `main`. This duplicate predates T-111 and was not introduced by it; the
T-119 heal was a one-time pass and T-110 was completed afterward, so it slipped
past the guard. Collapse the duplicate to a single heading, matching the shape
the T-119 healer produces, and confirm the corpus hygiene check is green.

Discovered during T-111 review (go-reviewer flagged the failing full suite).

## Acceptance

- `planning/tasks/T-110-next-include-off-spec.md` has exactly one
  `## Implementation Notes` heading, preserving the existing note bullets beneath it.
- `task check:task-bodies` passes (no task file carries a duplicate
  `## Implementation Notes` heading).
- `taskrail validate` passes; no other task file is modified.

## Verification Notes

- TODO: record verification evidence paths.

## Implementation Notes
