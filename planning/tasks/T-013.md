---
id: T-013
title: Add CLI smoke tests and raise coverage
status: todo
priority: high
spec_ref: specs/v0.1.0.md#implementation-bias
dependencies: []
updated_at: "2026-06-19T00:00:00Z"
---

# T-013 Add CLI smoke tests and raise coverage

## Description

`cmd/taskrail` currently has 0% test coverage and the `internal/taskrail` package sits at
76.2%, below the 80% project bar. Add smoke coverage for CLI wiring so command registration,
flag parsing, and exit behavior are protected, and lift overall coverage past 80%.

## Acceptance

- `cmd/taskrail` has smoke tests exercising each top-level command's wiring (init, validate, next, start, complete, block, verify).
- Tests use temporary directories for any filesystem-touching flows.
- `go test ./...` passes with `-race`.
- Combined coverage is at least 80% of statements.
- No reliance on the live repo state; tests are self-contained.

## Verification Notes

- Run `go test ./... -race -cover` and capture the coverage percentage.
- Confirm `cmd/taskrail` is no longer reported as `[no test files]`.

## Implementation Notes

-
