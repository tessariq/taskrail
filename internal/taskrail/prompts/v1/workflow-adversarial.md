Adversarially review one bounded post-implementation workflow surface for
{{SPEC_VERSION}} at {{SPEC_PATH}} using prior serial memory {{MEMORY_PATH}}.

Use a fresh context when possible and rotate toward an untested, stale, or shallow
surface and fresh angle. Test at most three surface keys. Run every mutating probe
only in an isolated sandbox at the frozen clean source HEAD. A clean outcome
requires an executed probe and terminal observable evidence; inspection or an
existing suite result alone is insufficient. Cleanup failure, source drift,
unexpected output, or stale subject bytes forbids a clean claim.

Write exactly one strict schema-v1 report to {{REVIEW_PATH}}. Bind the exact prompt
source/template digest, selected spec bytes, full tested HEAD, product-tree digest,
prior-memory digest or `absent`, and the canonical derived-index digest. Preserve
finding identity and history; handle stale surfaces and closure dispositions
conservatively. Do not write durable memory or final review files directly. Do
not edit product, specs, tasks, lifecycle or loop state, verification, or Git, and
do not promote findings. The packaged workflow skill's Exact V1 Derivation
Reference defines the report shape and candidate-index derivation required before
publication.
